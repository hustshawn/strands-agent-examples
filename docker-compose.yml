version: '3'

services:
  app:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - .:/workspace
    working_dir: /workspace
    command: /bin/bash
    environment:
      - PYTHONPATH=/workspace
    tty: true
    stdin_open: true